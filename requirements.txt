# Python package dependencies for IT Dashboard
# Add packages as you install them:
#   pip freeze > requirements.txt
# Install on another machine:
#   pip install -r requirements.txt
# Python package dependencies for IT Dashboard
psutil==5.9.8  # Or just 'psutil' to grab the latest version